#!/bin/bash

if [ $# -eq 0 ]
    then
        echo "No arguments supplied, first argument is name of player"
        exit 
fi

source wor_init

owner=wsc.code
table=itemproofs

cleos -u $IP get table $owner $1 $table 
